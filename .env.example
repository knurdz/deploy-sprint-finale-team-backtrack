# Public label — safe for client / build injection
VITE_PUBLIC_DEPLOY_LABEL=Deploy Sprint Finale v1.0

# Private token — GitHub Secret ONLY (Never use VITE_ prefix!)
# Set PRIVATE_DEPLOY_TOKEN in GitHub Secrets, not in git
