name: deploy-sprint-backtrack

services:
  team-site:
    image: deploy-sprint/team-site:${APP_VERSION:-latest}
    restart: unless-stopped
    ports:
      - "${APP_PORT:-8080}:80"
    env_file:
      - .env
